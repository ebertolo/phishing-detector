"""Experiment layer: GridSearch + cross-validation orchestration.

Ties the model registry, feature modes, calibration, blending, threshold tuning,
metrics and MLflow logging into a single run-many-models loop.
"""
