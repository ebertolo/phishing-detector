# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# uv / venv
.venv/
uv.lock

# Project artifacts
# Anchored to the repo root so it only ignores the top-level saved-model
# artifacts dir, NOT the src/phishing/models/ source package (which an
# unanchored "models/" would also match at any depth).
/models/
mlruns/
mlartifacts/
mlflow.db
mlflow.db-journal
embeddings/
data/*.csv
data/*.parquet
!data/.gitkeep

# Library temp/working dirs (CatBoost, XGBoost, etc.)
catboost_info/
*.log

# Environment / secrets
.env
.env.*

# OS / editor
.DS_Store
.vscode/
.idea/
